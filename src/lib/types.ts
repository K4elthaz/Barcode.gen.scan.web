export interface InventoryItem {
    id: string
    name: string
    sku: string
    category: string
    quantity: number
    price: number
    supplier: string
    location: string
    minStockLevel: number
    barcode: string
  }
  